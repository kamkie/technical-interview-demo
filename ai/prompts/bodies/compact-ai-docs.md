Compact the standing AI instruction files.

Read `AGENTS.md`, `ai/DOCUMENTATION.md`, `ai/PROMPTS.md`, and top-level `ai/*.md` first.
Do not bulk-load `ai/archive/`, `ai/references/`, `ai/prompts/bodies/`, `ai/templates/`, or `ai/skills/`; inspect those only when a cross-reference or duplicated policy points there.
Check `WORKING_WITH_AI.md` only when human-facing workflow wording overlaps with the AI guidance being changed.
Keep each file role-distinct.
Find duplicated standing guidance with targeted searches before opening large files.
Move duplicated standing guidance into the best owning AI document.
Tighten wording, remove overlap, update cross-references, and summarize the compaction decisions.
Propose new files only if the current owners cannot stay role-distinct without them.
