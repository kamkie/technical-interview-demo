# Create Plan

Category: Planning
Slug: `create-plan`
Placeholders: <topic>

Create `.agents/plans/PLAN_<topic>.md` for <topic>.

Follow `.agents/references/planning.md`.
Set the plan lifecycle using `.agents/references/planning.md` instead of guessing.
Update `ROADMAP.md` in the same change so active work points to the new plan path and current status.
