# Workers

Worker reports live here when durable workflow state is needed.

Use stable file names shaped like `<plan_stem_or_topic>__<role>.md`.
Each worker report records completed activity, changed files, validation result, blockers, risks, follow-up, and integration readiness.
