# Verifications

Verifier sidecar outputs live here when durable workflow state is needed.

Use stable file names shaped like `<plan_stem_or_topic>__<role>.md`.
Each verification records commands run, result, relevant logs or summaries, skipped checks, blockers, and recommended next activity when validation fails.
