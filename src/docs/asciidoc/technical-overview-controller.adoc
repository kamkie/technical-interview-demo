= TechnicalOverviewController API
:toc:
:toclevels: 2
:sectanchors:
:source-highlighter: highlight.js

xref:index.adoc[Back to API overview]

This page documents the internal technical-overview and smoke-test endpoints exposed by `TechnicalOverviewController`.

These paths are intended for trusted internal or devops validation. They are no longer part of the supported external `/api/**` contract.

Use this page together with xref:upgrade-1x-to-2-0.adoc[Upgrade From 1.x To 2.0] when validating that the deployed edge still exposes only `/api/**` publicly. The checked-in repo reference for that boundary is `k8s/edge/public-api-ingress.yaml`.

== `GET /`

Returns a technical application overview that combines build metadata, git metadata, selected dependency versions, and important runtime configuration values.

The security section is the repo-owned runtime posture source for same-site CSRF and deployment-owned abuse protection. It reports `csrfEnabled=true`, the supported `csrfCookieName` and `csrfHeaderName`, plus the vendor-neutral edge or gateway controls expected for login bootstrap and unsafe internet-public writes.
Post-deploy smoke and restore-drill validation use this payload as the runtime posture source and pair it with `GET /api/session` plus an authenticated unsafe write when JDBC-backed session access is available.

include::{snippets}/technical/get-root/curl-request.adoc[]
include::{snippets}/technical/get-root/http-request.adoc[]
include::{snippets}/technical/get-root/http-response.adoc[]
include::{snippets}/technical/get-root/response-headers.adoc[]
include::{snippets}/technical/get-root/response-fields.adoc[]

== `GET /hello`

Returns a plain text smoke-check style response for trusted internal validation.

include::{snippets}/hello/get-hello/curl-request.adoc[]
include::{snippets}/hello/get-hello/http-request.adoc[]
include::{snippets}/hello/get-hello/http-response.adoc[]
include::{snippets}/hello/get-hello/response-headers.adoc[]
include::{snippets}/hello/get-hello/response-body.adoc[]

