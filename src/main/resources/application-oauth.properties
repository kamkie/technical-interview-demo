spring.config.activate.on-profile=oauth

# GitHub OAuth App configuration for Phase 5.1.
# Supply these through environment variables or a local non-committed properties file.
spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID:}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET:}
app.security.oauth.github.client-id=${GITHUB_CLIENT_ID:}
app.security.oauth.github.client-secret=${GITHUB_CLIENT_SECRET:}
spring.security.oauth2.client.registration.github.scope=read:user,user:email
spring.security.oauth2.client.registration.github.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
